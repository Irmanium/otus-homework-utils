Программа на основе файла 'users.csv' генерирует пользователей (добавляя UUID, биографию 'bio' и хэш от пароля 'password') и вставляет их в БД.

Запуск:
1) Добавить в корень файл users.csv
2) Запустить команду
```bash
go run ./...
```
